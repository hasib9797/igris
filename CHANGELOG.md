# Changelog

## 1.0.0

- Added a real clone-and-run Ubuntu install flow with `install.sh` and `uninstall.sh`.
- Hardened setup, service installation, config generation, and database initialization.
- Made the main dashboard modules execute real backend actions for overview, services, packages, logs, processes, firewall, users, and files.
- Tightened auth, terminal access, route validation, and packaging/runtime behavior.
